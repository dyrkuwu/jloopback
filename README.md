# jloopback

small java library for visualizing audio loopback on windows (made for [epitaph](epitaph.wtf))

build:
```
cmake -S . -B build
cmake --build build --config Release --target jar
```

i doubt someone would find this useful in this state, but if they do, nice :D
